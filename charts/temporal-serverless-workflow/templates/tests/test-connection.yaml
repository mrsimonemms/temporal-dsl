apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "temporal-dsl.fullname" . }}-test-connection"
  labels:
    {{- include "temporal-dsl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "temporal-dsl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
