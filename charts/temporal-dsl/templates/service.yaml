apiVersion: v1
kind: Service
metadata:
  name: {{ include "temporal-dsl.fullname" . }}
  labels:
    {{- include "temporal-dsl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "temporal-dsl.selectorLabels" . | nindent 4 }}
