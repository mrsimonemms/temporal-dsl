apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zigflow.fullname" . }}-test-connection"
  labels:
    {{- include "zigflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zigflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
